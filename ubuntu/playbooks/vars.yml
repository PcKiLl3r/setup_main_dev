username: "pckill3r"
email: "zorko.grega@gmail.com"
ssh_key_filename: "id_pckill3r_rsa"
config: "{{ lookup('ansible.builtin.env', 'XDG_CONFIG') }}"
home: "{{ lookup('ansible.builtin.env', 'HOME') }}"
personal: "{{ home }}/personal"
dev: "{{ personal }}/dev"
